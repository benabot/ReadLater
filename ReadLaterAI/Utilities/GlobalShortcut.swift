import AppKit
import SwiftUI
import Carbon.HIToolbox

// MARK: - ShortcutKey
// Juste une struct pour afficher le raccourci dans l'UI.
// Le raccourci est hardcodé ⌥⌘R dans l'AppDelegate.

struct ShortcutKey {
    static let displayString = "⌥⌘R"
}
